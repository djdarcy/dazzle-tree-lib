[pytest]
# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Test paths - include both main tests and performance tests
testpaths = tests

# Exclude directories from test discovery
norecursedirs = revisions

# Asyncio configuration
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

# Markers
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    asyncio: mark test as using asyncio
    benchmark: performance measurement tests that produce timing results
    interaction_sensitive: tests whose results are affected by prior test execution (run in isolation)
    stress: stress/load tests that create heavy system load
    flaky: tests sensitive to system load or timing

# Output options
addopts = 
    --strict-markers
    --tb=short

# Timeout for tests (in seconds)
# timeout = 300

# Warnings to ignore or treat as errors
filterwarnings =
    # Ignore deprecation warnings from external libraries
    ignore::DeprecationWarning
    # But show our own deprecation warnings
    default::DeprecationWarning:dazzletreelib.*